#include<stdio.h>

int main(){
    // Arithmetic Instructions - +,-,/,*
    //for ex:
    int b = 10;
    int c = 10;
    int z;
    z = b*c;
    printf("The value of z is %d",z);
    // This is Just Playing with some algebra using arithmetic
    
    return 0;
}