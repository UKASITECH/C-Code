#include<stdio.h>

int main(){
    // It is actually shorthand if-else
    int a = 2;
    a==2?printf("Umang is great"):printf("Umang is legend");
    // so here Umang is great will print if condition is true
    // and if condition is false it will return Umang is legend
    
    return 0;
}